# Findings a person has examined and cleared, one fingerprint per line.
#
# This file is not a way to quieten the scanner. Every line here is a claim that somebody opened the
# named line of the named commit and established it is not a credential, and the fingerprint is
# precise enough that clearing one finding clears nothing else — not the file, not the rule, not the
# path. A blanket path exclusion would have covered every file added under that directory
# afterwards, which is the failure mode this format exists to avoid.
#
# Note for anyone adding an entry: describe the finding, never paste it. The first version of this
# file quoted the offending line verbatim to explain it, and the scanner found the quotation. An
# explanation that reproduces its subject is a second finding, and it was right about that too.
#
# ── ce557ec, adapters/reasoner-gemini/src/fixtures/gemini.cassette.json:222 ──────────────────────
#
# Rule `generic-api-key`, on a sixteen-hex-character field that was named "key" at that commit.
#
# Sixteen hex characters beside a field with that name is what an API credential looks like from the
# outside, and the rule is right to say so. The value is a truncated SHA-256 of the model name and
# the canonical request — the lookup index of a recorded HTTP exchange, computed from a request
# committed in the same file. It authorises nothing and anyone with the repository can recompute it.
#
# The field is called `digest` from the next commit onward, because the old name was the ambiguous
# part and renaming it removes the ambiguity at its source rather than teaching the scanner to look
# away. The entry below covers only the commits where the old name was still in the file. A real
# Gemini credential never reaches a cassette at all: it travels as an `x-goog-api-key` header, and a
# cassette holds bodies. There is a test asserting that.
ce557ec9677c0b2241345a16a44a5ec69bb2ad86:adapters/reasoner-gemini/src/fixtures/gemini.cassette.json:generic-api-key:222
#
# ── 8cc9b09, .gitleaksignore:11 ─────────────────────────────────────────────────────────────────
#
# The same rule, on the version of this file that quoted the finding above rather than describing
# it. The quotation is gone from the working tree, and it is still in that commit, and a scan of
# history is right to keep finding it there — that is the whole point of scanning history rather
# than the checkout.
#
# Rewriting a pushed commit to make an embarrassing intermediate state disappear is the wrong
# instrument for this. The value is the same truncated SHA-256 as the entry above, cleared for the
# same reason, and the commit that removed it explains what happened. Both stay.
8cc9b09db7ee17da85fc32818961f15c094d889a:.gitleaksignore:generic-api-key:11

#
# ── 87ebcd5, apps/site/src/settings.ts:14 ───────────────────────────────────────────────────────
#
# `generic-api-key`, on the name of a browser storage slot. The site remembers which palette a
# reader chose, and the constant holding that slot's name carries a version suffix so that changing
# a default does not leave a stale choice outliving it. Hyphenated lowercase words and a digit are
# enough entropy for the generic rule, which is working as intended — it cannot know that the value
# is written to `localStorage` in the reader's own browser and never travels anywhere.
#
# Nothing is secret about it by construction: the whole file ships to the tab as readable source.
87ebcd5ecaa18699460032a937065d5659b7a1c0:apps/site/src/settings.ts:generic-api-key:14
