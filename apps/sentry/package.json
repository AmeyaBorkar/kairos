{
  "name": "@kairos/sentry",
  "version": "0.0.0",
  "private": true,
  "description": "Ingests payment outcomes, runs detection, publishes the current steering plan, and serves the operator view. Horizontally scalable.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@kairos/detect": "workspace:*",
    "@kairos/domain": "workspace:*",
    "@kairos/ledger": "workspace:*",
    "@kairos/policy": "workspace:*",
    "@kairos/postgres": "workspace:*",
    "@kairos/razorpay": "workspace:*",
    "@kairos/terminus": "workspace:*",
    "fastify": "^5.2.0",
    "pg": "^8.23.0",
    "throttlekit": "^1.7.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.23.1"
  }
}
