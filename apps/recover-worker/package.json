{
  "name": "@kairos/recover-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Drains the casualty queue under one mandate. Horizontally scalable; every bound it observes belongs to Terminus, not to it.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@kairos/domain": "workspace:*",
    "@kairos/ledger": "workspace:*",
    "@kairos/postgres": "workspace:*",
    "@kairos/razorpay": "workspace:*",
    "@kairos/reason": "workspace:*",
    "@kairos/recover": "workspace:*",
    "@kairos/terminus": "workspace:*",
    "pg": "^8.23.0",
    "throttlekit": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.23.1"
  }
}
