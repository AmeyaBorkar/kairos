{
  "name": "@kairos/detect",
  "version": "0.0.0",
  "private": true,
  "description": "Sequential change detection over payment outcomes. Pure: no I/O, no clock reads.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@kairos/domain": "workspace:*"
  }
}
