{
  "name": "@kairos/policy",
  "version": "0.0.0",
  "private": true,
  "description": "Steering decisions. Which rails to move traffic off, by how much, and who is held back as a control.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@kairos/domain": "workspace:*",
    "@kairos/terminus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
